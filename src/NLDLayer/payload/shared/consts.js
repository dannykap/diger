const INVOKE_STATUS = {
  PENDING: 'pending',
  IN_PROGRESS: 'in-progress',
  COMPLETED: 'completed',
  FAILED: 'failed',
};

const LAMBDA_NAME_KEY = 'mirrorClient';

module.exports = {
  INVOKE_STATUS,
  LAMBDA_NAME_KEY,
};

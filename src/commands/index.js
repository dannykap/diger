const deploy = require('./deploy');
const connect = require('./connect');
const detach = require('./detach');
const generate = require('./generate');

module.exports = { deploy, connect, detach, generate };
